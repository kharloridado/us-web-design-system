---
name: mobile-ui-design-tokens
description: Mobile UI design tokens — color/spacing/typography/border/elevation values exposed as --token-* CSS variables. Use when theming a Mobile UI app, picking a token for a CSS rule, or overriding tokens for branding/dark-mode. Tokens are Mobile UI-only — OutSystems UI uses its own --color-*/--space-* variables.
---

# Mobile UI Design Tokens

Design tokens (--token-*) for Mobile UI — color, spacing, typography, border, elevation. The full catalog plus how to override for branding and dark mode. Tokens are Mobile UI-only; OutSystems UI uses different variables.

**Canonical doc:** [`foundations/outsystems-design-tokens/design-tokens.md`](../../../foundations/outsystems-design-tokens/design-tokens.md)

The full content lives in the canonical doc — open it for the complete reference. This wrapper exists so agents that load skills from `.claude/skills/` (Claude Code, Cursor, etc.) discover this content via its description, then follow the link.
