---
name: atomic-design-methodology
description: Atomic Design composition methodology applied to OutSystems — atoms (built-in widgets), molecules (small Blocks), organisms (composite Blocks), templates (Layouts + Screen Templates), pages. Use when deciding whether to extract a custom Block, where to put logic, or how reusable a UI piece should be.
---

# Atomic Design Methodology

Composition methodology mapped to OutSystems building blocks — when to extract a custom Block, when not to, where logic and state belong, page-level edge cases.

**Canonical doc:** [`common/atomic-design.md`](../../../common/atomic-design.md)

The full content lives in the canonical doc — open it for the complete reference. This wrapper exists so agents that load skills from `.claude/skills/` (Claude Code, Cursor, etc.) discover this content via its description, then follow the link.
