---
name: outsystems-css-customization
description: CSS customization in OutSystems — where to put styles (Theme > Screen > Block), how to override framework CSS variables, design-token-vs-framework-variable distinction (Mobile UI uses --token-*, OutSystems UI uses its own), anti-patterns. Use when writing custom CSS, theming an app, or implementing dark mode.
---

# Outsystems Css Customization

Where custom CSS belongs (Application Theme first), how to override framework CSS variables per stack (OutSystems UI vs Mobile UI), dark-mode pattern, and anti-patterns.

**Canonical doc:** [`common/css-customization.md`](../../../common/css-customization.md)

The full content lives in the canonical doc — open it for the complete reference. This wrapper exists so agents that load skills from `.claude/skills/` (Claude Code, Cursor, etc.) discover this content via its description, then follow the link.
