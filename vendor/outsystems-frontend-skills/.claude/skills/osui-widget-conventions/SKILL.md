---
name: osui-widget-conventions
description: Critical OutSystems UI widget conventions — content vs Widgets keys, TableRecords structure, qualified expression paths, FULL PATH parameter naming, event wiring. Use when generating widget JSON or debugging silent failures (empty widgets, missing columns, ignored styles).
---

# OSUI Widget Conventions

Ten conventions that cause silent failures when violated (empty widgets, zero columns, ignored styles, broken events). Mandatory reading when generating widget JSON.

**Canonical doc:** [`ui-frameworks/outsystems-ui/widget-conventions.md`](../../../ui-frameworks/outsystems-ui/widget-conventions.md)

The full content lives in the canonical doc — open it for the complete reference. This wrapper exists so agents that load skills from `.claude/skills/` (Claude Code, Cursor, etc.) discover this content via its description, then follow the link.
